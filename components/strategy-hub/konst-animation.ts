/** Tokeny animacji widoków konstelacji (nitka, blueprint, warstwa decyzji). */
export const SPRING_CAMERA = { stiffness: 260, damping: 28 } as const;
export const SPRING_POP = { stiffness: 300, damping: 22 } as const;
export const DRAW_EDGE = { duration: 0.5, ease: "easeOut" as const };
export const STAGGER_THREAD = 0.12;
export const STAGGER_COLUMN = 0.06;
export const FADE_DIM = { duration: 0.2, ease: "easeOut" as const };
export const HALO_IN = { duration: 0.3, ease: "easeOut" as const };
export const PANEL_SLIDE = { duration: 0.2, ease: "easeOut" as const };
